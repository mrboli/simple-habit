const lesson = (state = {}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

const lessons = (state = [], action) => {
  switch (action.type) {
    default:
        return state
  }
}

export default lessons
